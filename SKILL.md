# Commit and Push Skill
1. Stage all changed files with `git add -A`
2. Write a concise conventional commit message
3. Commit the changes
4. Push to the current remote branch
5. Verify the push succeeded by running `git log --oneline -1` and `git status`
